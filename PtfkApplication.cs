using Microsoft.AspNetCore.Hosting;
using Microsoft.Extensions.Configuration;
using PetaframeworkStd;
using System;

namespace Petaframework
{
    public static class PtfkApplication
    {
        //public static void Start(IConfiguration configuration, IWebHostEnvironment env = null)
        //{
        //    Petaframework.Strict.ConfigurationManager.Set(configuration);
        //    PtfkConsole.Set(
        //                () => configuration.GetValue<bool>("AppConfiguration:Debug.PtfkConsole", false),
        //                () => configuration.GetValue<string>("AppConfiguration:Debug.Session.Ids.ToTrace", "*"));
        //}
    }
}